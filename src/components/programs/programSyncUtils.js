/**
 * Program Management tab session keys.
 *
 * These keys are retained for tab persistence and one-time forced tab selection.
 * No reload-to-sync behavior remains in online-only mode.
 */

export const PROGRAM_ACTIVE_TAB_KEY = "programManagement.activeTab";
export const PROGRAM_FORCE_TAB_KEY = "programManagement.forceTabAfterReload";
